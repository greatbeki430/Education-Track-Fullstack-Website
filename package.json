{
  "scripts": {
    "build": "cd frontend && npm install && npx vite build"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cookie-parser": "^1.4.7",
    "csurf": "^1.11.0"
  }
}
